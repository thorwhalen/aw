[tool.pytest.ini_options]
markers = [
    "real_llm: marks tests that use real LLM APIs (deselect with '-m \"not real_llm\"')",
]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
